// This file is automatically generated from state.jq. Do not edit manually. Use ./scripts/generate-jq.bat to regenerate it.
const EVCC_JQ_FILTER = "def avg: if length > 0 then add / length else null end; def unix_timestamp: if type == \"number\" then . elif type == \"string\" then capture( \"^(?<date>[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2})(?:\\\\.[0-9]+)?(?<timezone>Z|[+-][0-9]{2}:[0-9]{2})$\" ) as $parts | ( $parts.date | strptime(\"%Y-%m-%dT%H:%M:%S\") | mktime ) as $base | if $parts.timezone == \"Z\" then $base else ($parts.timezone[0:1]) as $sign | ($parts.timezone[1:3] | tonumber) as $hours | ($parts.timezone[4:6] | tonumber) as $minutes | (($hours * 3600) + ($minutes * 60)) as $offset | if $sign == \"+\" then $base - $offset else $base + $offset end end else null end; def normalize_grid: [ (. // [])[] | if type == \"array\" and length >= 3 then (.[0] | unix_timestamp) as $start | (.[1] | unix_timestamp) as $end | select($start != null and $end != null) | [ $start, $end, .[2] ] elif type == \"object\" then (.start | unix_timestamp) as $start | (.end | unix_timestamp) as $end | select($start != null and $end != null) | [ $start, $end, .value ] else empty end ]; def grid_avg_between($grid; $from; $to): [ ($grid // [])[] | select(.[0] >= $from and .[0] < $to) | .[2] ] | avg; def local_day($timestamp): $timestamp | strflocaltime(\"%Y-%m-%d\"); def price_period($grid; $slots; $mode; $from): [ ($grid // [])[] | select(.[0] >= $from) ] as $entries | if ($entries | length) >= $slots then [ range(0; ($entries | length) - $slots + 1) as $i | ($entries[$i:$i + $slots]) as $window | { start: $window[0][0], end: $window[-1][1], average: ( ($window | map(.[2]) | add) / $slots ) } ] | if $mode == \"max\" then max_by(.average) else min_by(.average) end else null end; . as $root | ($root.result // $root) as $data | now as $now | ($data.forecast.grid | normalize_grid) as $grid | { batteryPower: $data.batteryPower, batterySoc: $data.batterySoc, battery: if ($data.battery | type) == \"object\" then { power: $data.battery.power, soc: $data.battery.soc } else null end, forecast: { grid: { next60MinutesAverage: ( grid_avg_between( $grid; $now; $now + 3600 ) ), next60To120MinutesAverage: ( grid_avg_between( $grid; $now + 3600; $now + 7200 ) ), remainingTodayAverage: ( local_day($now) as $day | [ $grid[] | select( .[0] >= $now and local_day(.[0]) == $day ) | .[2] ] | avg ), tomorrowAverage: ( local_day($now + 86400) as $day | [ $grid[] | select( local_day(.[0]) == $day ) | .[2] ] | avg ), cheapest1h: ( price_period( $grid; 4; \"min\"; $now ) ), cheapest2h: ( price_period( $grid; 8; \"min\"; $now ) ), cheapest3h: ( price_period( $grid; 12; \"min\"; $now ) ), mostExpensive1h: ( price_period( $grid; 4; \"max\"; $now ) ) } , solar: ( $data.forecast.solar | { scale, today: { energy: .today.energy }, tomorrow: { energy: .tomorrow.energy }, dayAfterTomorrow: { energy: .dayAfterTomorrow.energy } } ) }, gridPower: $data.gridPower, grid: { power: $data.grid.power }, homePower: $data.homePower, loadpoints: [ ( $data.loadpoints[] | { chargePower, chargerFeatureHeating, chargerFeatureIntegratedDevice, charging, connected, vehicleTitle, vehicleSoc, title, phasesActive, mode, chargeRemainingDuration } ) ], pvPower: $data.pvPower, siteTitle: $data.siteTitle, smartCostAvailable: $data.smartCostAvailable, statistics: ( $data.statistics | map_values({ solarPercentage }) ), tariffGrid: $data.tariffGrid, vehicles: ( $data.vehicles | map_values({ title }) ) } | walk( if type == \"object\" then with_entries( select( .value != null and .value != {} and .value != [] ) ) elif type == \"array\" then map( select( . != null and . != {} and . != [] ) ) else . end )";
